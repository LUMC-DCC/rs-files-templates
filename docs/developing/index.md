# Developing

The package is intentionally small: typed models define file inputs, Jinja templates define textual
layout, and the renderer connects the two. Structured output schemas and the upstream context
contract are validation tools.
